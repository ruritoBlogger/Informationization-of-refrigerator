<p>メインページ</p>
<h3>ピックアップ情報</h3>
<p>賞味期限が迫っているもの</p>
<table>
  <tr>
    <th>名前</th>
    <th>期限</th>
    <th>総量</th>
    <th>残り量</th>
    <th>詳細</th>
  </tr>

  <% @oldfoods.each do |food| %>
    <tr>
      <td><%= food.name %></td>
      <td><%= food.limitMonth %>月<%= food.limitDay %>日</td>
      <td><%= food.amount %><%= food.amounttype %></td>
      <td><%= food.yetamount %></td>
      <td><%= link_to("詳細", "/food/#{food.id}/show") %></td>
    </tr>
  <% end %>
</table>


<p>消費期限の食べ物</p>
<table>
  <tr>
    <th>名前</th>
    <th>期限</th>
    <th>総量</th>
    <th>残り量</th>
    <th>詳細</th>
  </tr>

  <% @Fleshfoods.each do |food| %>
    <tr>
      <td><%= food.name %></td>
      <td><%= food.limitMonth %>月<%= food.limitDay %>日</td>
      <td><%= food.amount %><%= food.amounttype %></td>
      <td><%= food.yetamount %></td>
      <td><%= link_to("詳細", "/food/#{food.id}/show") %></td>
    </tr>
  <% end %>
</table>

<p>メモ</p>
未実装
・Twitterとの連動
・グーグルカレンダーとの連動
・スマホアプリ化
・Webスクレイピングして料理の情報を関連付けて表示

・食べ物の種類の編集ページ
・食べ物の情報の更新ページで残り量以外の情報を更新するページ
・jQueryの再実装（現在挙動不審なため要点検)
・CSSの編集
・レベルの高さを用いてランキングページのソート
・不正アクセスの制限
・パスワードを暗号化して保存

実装中
・食べ物の画像の登録
・食べ物の新規登録の時、食べ物の種類の登録

